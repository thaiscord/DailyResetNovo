Use the image named exactly:

ICON

located inside:

Daily Reset App / Identidade Visual

This image must become the ONE AND ONLY official app icon for:

* PWA icon
* Web app icon
* Apple home screen icon
* Android home screen icon
* Manifest icons
* Favicon

Requirements:

1. Do not use any other logo.
2. Do not use any Vercel default icon.
3. Do not generate a new icon.
4. Do not redesign the icon.
5. Use the ICON image exactly as provided in the Identidade Visual folder.
6. Generate all required sizes from that image:

   * favicon
   * 180x180 apple-touch-icon
   * 192x192
   * 512x512
   * any other required PWA sizes
7. Update:

   * manifest.json
   * app.json / app.config
   * web metadata
   * favicon references
   * apple-touch-icon references
8. Remove every reference to the current Vercel/default icon.
9. Keep the app name as:
   Daily Reset

After implementation, provide:

* the exact files modified
* the exact icon file paths being used
* confirmation that the ICON image from Identidade Visual is now the official app icon everywhere.
